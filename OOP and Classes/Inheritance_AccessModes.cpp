#include <iostream>

using namespace std;

int main()
{
	/*
	  We can use public, private, protected modes to inherit a class.
	  
	  Example:
	     class Animal{
			 
		 };
		 class Dog: private Animal{
			 
		 };
		 class Cat: protected Animal{
			 
		 };
		 
        Public: If a derived class is declared in this mode, then the mebers of the base
            class are inherited by the derived class just as they are.

        Private: In this case, all the members of the base class bkam private members in the
             derived class.
             
        Protected: The public members of the base class bkam protected members in the derived class.
		 
      	*/
	
	return 0;
}